% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extrapolate.R
\name{extrapolate_treatseqr}
\alias{extrapolate_treatseqr}
\title{Extrapolate Health Economic Sequence Model}
\usage{
extrapolate_treatseqr(m, spec)
}
\arguments{
\item{m}{A list containing the transition matrices:
\itemize{
\item m1: A numeric array of dim c(th, n_pre, n_dest) holding the pre-tunnel
transition probabilities, indexed
\verb{[cycle, pre-tunnel state, destination]}.
Destinations are in canonical order (all states, then death).
\item m1_dest: Integer vector giving the column of the full matrix M that each
destination slot of m1 corresponds to.
\item m2: A sparse matrix for tunnel states.
\item state_names: Character vector of state names, death last.
}}

\item{spec}{A model specification list, as returned by \code{specify_m()}.
Only two of its fields are used here:
\itemize{
\item matrix_size: Size of the transition matrix.
\item pre_tunnels: Number of pre-tunnel states.
}}
}
\value{
A matrix where rows represent states and columns represent time
points (cycles). The matrix shows the proportion of the cohort in each
state at each time point.
}
\description{
This function extrapolates a health economic sequence model using sparse
matrices. It generates a population trace matrix showing state occupancy
over time.
}
\examples{
spec <- specify_m(tunnel_lengths = c(3, 3), pre_tunnel_states = 1)

tp <- list(
  pre  = list(tun1 = rep(0.2, 3), tun2 = rep(0.1, 3), die = rep(0.05, 3)),
  tun1 = list(tun2 = rep(0.3, 3), die = rep(0.1, 3)),
  tun2 = list(die = rep(0.2, 3))
)
state_names <- c("pre", "tun1", "tun2", "die")

m <- generate_m_list(spec, tp, state_names)
trace <- extrapolate_treatseqr(m, spec)

# rows are states, columns are time points (cycle 0 first)
dim(trace)

# the cohort is conserved, so every column sums to 1
colSums(trace)
}
